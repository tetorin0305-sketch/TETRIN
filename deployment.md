# オンライン対戦サーバーのデプロイガイド

このテトリスをインターネット上の外部サーバーで公開し、誰とでも対戦できるようにするための手順を説明します。

## 推奨されるホスティングサービス

以下のサービスは Node.js アプリケーションを簡単に公開でき、無料で開始できるプランがあります。

1.  **Render (render.com)** - 最も簡単で推奨されます。
2.  **Fly.io** - パフォーマンスが良いですが、クレジットカード登録が必要です。
3.  **Glitch** - ブラウザ上ですべて完結します。

---

## デプロイ手順 (Render の場合)

1.  **GitHub にコードをアップロード**:
    このプロジェクトのファイルをすべて GitHub リポジトリにプッシュします。

2.  **Render でログイン**:
    [render.com](https://render.com) にアクセスし、GitHub アカウントでログインします。

3.  **New -> Web Service を選択**:
    GitHub リポジトリを選択します。

4.  **設定を入力**:
    - **Name**: `my-tetris-game` (任意)
    - **Environment**: `Node`
    - **Build Command**: `npm install`
    - **Start Command**: `npm start`

5.  **デプロイ開始**:
    「Create Web Service」をクリックします。数分で URL (`https://my-tetris-game.onrender.com` など) が発行されます。

---

## 友達と対戦する方法

1.  発行された URL にアクセスします。
2.  「対戦モード」->「Online」を選択します。
3.  「ルーム名」を入力して「作成」を押します。
4.  ロビー画面で**「招待リンクをコピー」**をクリックします。
5.  コピーされた URL を対戦相手（友達）に送ります。
6.  友達がその URL を開くと**自動的に同じルームに入室**します。
7.  両者が「準備完了」になれば試合開始です！

## トラブルシューティング

-   **接続できない場合**: デプロイ先のサーバーが HTTPS であることを確認してください（通常は自動です）。
-   **ポート番号**: サーバー側の `process.env.PORT` は自動的に環境に合わせて設定されるよう実装済みです。
